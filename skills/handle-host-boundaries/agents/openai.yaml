interface:
  display_name: "Handle Safety Boundaries"
  short_description: "Handle unavailable capabilities and destructive roots"
  default_prompt: "Use $handle-host-boundaries to handle this host or destructive boundary safely."
